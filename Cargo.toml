[package]
name = "drive2"
version = "0.2.0"
edition = "2021"
description = "DRIVE GAME2 (MSX・FAN ファンダム掲載 / HASEMAKO) の Rust 移植"
license = "MIT"

[dependencies]
macroquad = { version = "0.4", default-features = false }
tinyaudio = "2"
